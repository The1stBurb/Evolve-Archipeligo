dat=['<div>${loc(type)}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div>${loc(\'portal_turret_effect\',[rating])}</div><div class="has-text-caution">${loc(\'minus_power\',[power])}</div>', '<div>${loc(\'portal_war_droid_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div>${loc(\'portal_war_droid_effect\')}</div><div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${loc(\'portal_repair_droid_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div>${loc(\'portal_repair_droid_effect\',[5])}</div><div>${loc(\'portal_repair_droid_effect2\',[8])}</div><div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', ' <span class="has-text-danger">${loc(\'portal_minions_bd\')}:</span> <span class="has-text-caution">{{ spawns | approx }}</span>', '<div>${loc(\'portal_war_drone_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div>${loc(\'portal_war_drone_effect\')}</div><div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${loc(\'portal_sensor_drone_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc('space_moon_observatory_cata_effect',[2])}</div>", "<div>${loc('space_moon_observatory_effect',[2])}</div><div>${loc('portal_sensor_drone_effect2',[2])}</div>", "<div>${loc('city_max_knowledge',[know])}</div>${sci_bonus}", '<div>${loc(\'portal_sensor_drone_effect\',[bonus])}</div>${sci}<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${loc(\'portal_attractor_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div>${loc(\'portal_attractor_effect1\')}</div><div>${loc(\'portal_attractor_effect2\',[global.resource.Soul_Gem.name])}</div><div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', "<div>${loc('portal_minions_effect',[low_troops,troops])}</div>", "<div>${loc('plus_max_resource',[1,global.resource.Authority.name])}</div>", "<div>${loc('portal_reaper_effect')}</div>", "<div>${loc('portal_corpse_pile_effect',[power,loc('portal_incinerator_title')])}</div>", '<div>${loc(\'portal_mortuary_desc\',[loc(\'portal_corpse_pile_title\')])}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div>${loc(`eden_ascension_machine_effect1`,[loc(`eden_encampment_title`),+omniscience.toFixed(0),global.resource.Omniscience.name])}</div>', '<div>${loc(`eden_ascension_machine_effect2`,[loc(`job_ghost_trapper`),+ghost.toFixed(2)])}</div>', '<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', "<div>${loc('portal_codex_effect',[])}</div>", '<div class="has-text-${global.resource.Money.amount >= $(this)[0].cost.Money() ? \'success\' : \'danger\'}">${loc(\'portal_codex_money\',[sizeApproximation(global.resource.Money.amount),sizeApproximation($(this)[0].cost.Money())])}</div>', '<div class="has-text-${global.resource.Furs.amount >= $(this)[0].cost.Furs() ? \'success\' : \'danger\'}">${loc(\'portal_codex_res\',[sizeApproximation(global.resource.Furs.amount),sizeApproximation($(this)[0].cost.Furs()),global.resource.Furs.name])}</div>', '<div class="has-text-${global.portal.minions?.spawns >= 3000 ? \'success\' : \'danger\'}">${loc(\'portal_codex_res\',[(global.portal.minions?.spawns || 0),3000,loc(\'portal_codex_demon\')])}</div>', '<div class="has-text-${global.portal.codex?.s >= 10 ? \'success\' : \'danger\'}">${loc(\'portal_codex_res\',[(global.portal.codex?.s || 0),10,loc(\'portal_codex_sac\')])}</div>', "<div>${loc('plus_max_resource',[sizeApproximation(knowCap),global.resource.Knowledge.name])}</div>", "<div>${loc('city_library_effect',[Math.round((global.race?.absorbed?.length || 1) * 10 * know)])}</div>", "<div>${loc('plus_res_duo',[500,global.resource.Crates.name,global.resource.Containers.name])}</div>", "<div>${loc('plus_max_resource',[(global.race?.absorbed?.length || 1),global.resource.Authority.name])}</div>", "<div>${loc('portal_throne_of_evil_effect',[essense])}</div>", '<div class="has-text-success">${loc(\'portal_throne_of_evil_capture\',[hearts])}</div>', '<div class="has-text-danger">${loc(\'portal_throne_of_evil_capture2\',[races[global.portal.throne.hearts[0]].name])}</div>', '<div class="has-text-info">${loc(\'portal_throne_of_evil_skill2\')} ${loc(\'portal_throne_of_evil_skill\',[global.portal.throne.points])}</div>', '<div class="has-text-info">${loc(\'portal_throne_of_evil_skill1\')} ${loc(\'portal_throne_of_evil_skill\',[global.portal.throne.points])}</div>', "<div>${loc('space_dwarf_reactor_effect1',[-($(this)[0].powered(wiki))])}</div>", "<div>${loc('portal_incinerator_effect',[15 * rank,loc('portal_twisted_lab_title'),global.resource.Graphene.name])}</div>", '<div class="aTable">', "<span>${loc('plus_max_resource',[val,global.resource[res].name])}</span>", "<span>${loc('plus_max_resource',[65 + (global.portal?.warehouse?.rank || 1) * 35, global.resource.Crates.name])}</span>", "<span>${loc('plus_max_resource',[65 + (global.portal?.warehouse?.rank || 1) * 35, global.resource.Containers.name])}</span>", '</div>', "<div>${loc('plus_max_resource',[pop,loc('citizen')])}</div><div>${loc('city_grove_effect',[2.5])}</div>", '<div>${rankDesc(structName(\'casino\'),\'hell_casino\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${rankDesc(loc(\'portal_twisted_lab_title\'),\'twisted_lab\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc('plus_max_resource',[(+know.toFixed(0)).toLocaleString(),global.resource.Knowledge.name])}</div>", "<div>${loc('city_university_effect',[jobScale(3)])}</div>", "<div>${loc('plus_max_resource',[jobScale(2),jobName('scientist')])}</div>", "<div>${loc('interstellar_g_factory_effect')}</div>", '<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${rankDesc(loc(\'portal_demon_forge_title\'),\'demon_forge\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc('city_foundry_effect1',[jobScale($(this)[0].crafters())])}</div><div>${loc('interstellar_stellar_forge_effect',[$(this)[0].crafting()])}</div>", "<div>${loc('interstellar_stellar_forge_effect3',[num_smelters])}</div>", '${desc}<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${rankDesc(loc(\'portal_factory_title\'),\'hell_factory\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc('portal_factory_effect',[$(this)[0].lines()])}</div><div>${loc('city_crafted_mats',[25])}</div>", "<div>${loc('plus_max_resource',[jobScale(5),jobName('cement_worker')])}</div>", "<div>${loc('production',[(global.portal?.hell_factory?.rank || 1) * 8 - 8,global.resource.Cement.name])}</div>", '<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', "<div>${loc('plus_res_combo',[oil,oc,global.resource.Oil.name])}</div>", "<div>${loc('plus_res_combo',[helium,storage,global.resource.Helium_3.name])}</div>", "<div>${loc('city_oil_well_bodies',[+(global.city.oil_well.dead).toFixed(1),50 * maxDead])}</div>", "<div>${loc('city_oil_well_consume',[traits.blubber.vars()[0]])}</div>", "<div>${loc('portal_tunneler_effect',[boost])}</div>", "<div>${loc('portal_tunneler_effect2')}</div>", "<div>${loc('plus_max_soldiers',[troops])}</div>", "<div>${loc('plus_max_resource',[1,global.resource.Authority.name])}</div>", '<div>${loc(\'portal_soul_forge_desc\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc(global.race['warlord'] ? 'portal_soul_forge_warlord' : 'portal_soul_forge_effect',[global.resource.Soul_Gem.name])}</div>", "<div>${loc('portal_soul_forge_effect2',[global.portal['soul_forge'] ? global.portal.soul_forge.kills.toLocaleString() : 0,Math.round(cap).toLocaleString()])}</div>", '${desc}<div><span class="has-text-caution">${loc(\'portal_soul_forge_soldiers\',[soldiers])}</span>, <span class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</span></div>', '<div>${loc(\'portal_gun_emplacement_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div>${loc(\'portal_gun_emplacement_effect\',[soldiers])}</div><div>${loc(\'portal_gun_emplacement_effect2\',[min,max])}</div><div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${loc(\'portal_soul_attractor_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc('portal_soul_attractor_effect',[low, high])}</div>", "<div>${loc('portal_soul_attractor_effect2',[global.stats.achieve['what_is_best'] && global.stats.achieve.what_is_best.e >= 3 ? 4 : 3])}</div>", "<div>${loc('production',[global.tech.pitspawn >= 3 ? 20 : 10,loc('portal_shadow_mine_title')])}</div>", '<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${loc(\'portal_soul_capacitor_desc\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc('portal_soul_capacitor_effect',[energy.toLocaleString()])}</div>", "<div>${loc('portal_soul_capacitor_effect2',[eCap.toLocaleString()])}</div>", "<div>${loc('portal_soul_capacitor_effect3',[cap.toLocaleString()])}</div>", '<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${loc(\'portal_absorption_chamber_title\')}</div><div class="has-text-special">${loc(\'requires_segments\',[100])}</div>', "<div>${loc('portal_absorption_chamber_title')}</div>", '<div>${loc(\'portal_absorption_chamber_incomplete\')}</div><div class="has-text-special">${loc(\'space_dwarf_collider_effect2\',[remain])}</div>', '<div>${loc(\'portal_absorption_chamber_effect_eld\',[(100000000).toLocaleString()])}</div><div class="has-text-special">${loc(\'tech_demonic_infusion_effect2\',[calcPrestige(\'descend\').artifact])}</div>', '<div>${loc(`portal_absorption_chamber_effect`,[(100000000).toLocaleString()])}</div><div>${reward}</div>', '<div>${loc(\'portal_shadow_mine_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc('gain',[+(elerium).toFixed(3), global.resource.Elerium.name])}</div>", "<div>${loc('gain',[+(infernite).toFixed(3), global.resource.Infernite.name])}</div>", "<div>${loc('gain',[+(vitreloy).toFixed(3), global.resource.Vitreloy.name])}</div>", "<div>${loc('plus_max_resource',[elerium_cap, global.resource.Elerium.name])}</div>", '<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', '<div>${loc(\'portal_tavern_title\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', "<div>${loc('plus_resource_per',[0.35,loc('morale'),loc('portal_shadow_mine_title')])}</div>", '<div class="has-text-caution">${loc(\'minus_power\',[$(this)[0].powered()])}</div>', ' - <span class="has-text-advanced">${loc(\'portal_ruins_security\')}:</span> <span class="has-text-caution">{{ on | filter(\'army\') }}</span>', ' - <span class="has-text-advanced">${loc(\'portal_ruins_supressed\')}:</span> <span class="has-text-caution">{{ on | filter(\'sup\') }}</span>', '<div>${loc(\'portal_guard_post_title\')}</div><div class="has-text-special">${loc(\'requires_soldiers\')}</div><div class="has-text-special">${loc(\'requires_power\')}</div>', '<div>${loc(\'portal_guard_post_effect1\',[rating])}</div><div class="has-text-caution">${loc(\'portal_guard_post_effect2\',[jobScale(1),$(this)[0].powered()])}</div>', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_ruins: 2 },\n            not_trait: [', '); },\n            },\n            powered(){ return powerCostMod(8); },\n            effect(){\n                return `<div>${loc(', ',[jobScale(2)])}</div><div class="has-text-caution">${loc(', ',[$(this)[0].powered()])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { housing: 4 },\n            not_trait: [', ', 0, wiki);\n                let vault = spatialReasoning(bank_vault() * 8 * sup.supress);\n                vault = +(vault).toFixed(0);\n                let containers = Math.round(get_qlevel(wiki)) * 10;\n                let container_string = `<div>${loc(', ',[containers,global.resource.Crates.name])}</div><div>${loc(', ',[containers,global.resource.Containers.name])}</div>`;\n                return `<div>${loc(', ')])}</div><div>${loc(', ',[$(this)[0].citizens()])}</div><div>${loc(', ')])}</div><div>${loc(', ',[75])}</div>${container_string}<div class="has-text-caution">${loc(', ',[$(this)[0].powered()])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '); },\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { scarletite: 1 },\n            cost: {\n                Money(offset){ return spaceCostMultiplier(', '] ? `<div>${loc(', '] && global.stats.achieve.what_is_best.e >= 1 ? 12 : 10,loc(`portal_inferno_power_title`)])}</div>` : ``;\n                return `<div>${loc(', ',[jobScale(1)])}</div>${reactor}<div>${loc(', ',[$(this)[0].smelting()])}</div><div>${loc(', ',[craft])}</div><div class="has-text-caution">${loc(', ',[$(this)[0].powered()])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '),\n            desc(){\n                return `<div>${loc(', ')}</div>`;\n            },\n            reqs: { inferno_power: 1 },\n            cost: {\n                Money(offset){ return spaceCostMultiplier(', '] && global.stats.achieve.what_is_best.e >= 1 ? 12 : 10); \n                }\n                return powerModifier(-(power));\n            },\n            fuel: {\n                Infernite: 5,\n                Coal: 100,\n                Oil: 80\n            },\n            effect(wiki){\n                let fuel = $(this)[0].fuel;\n                return `<div>${loc(', ',[-($(this)[0].powered(wiki))])}</div><div class="has-text-caution">${loc(', ',[fuel.Infernite,global.resource.Infernite.name,fuel.Coal,global.resource.Coal.name,fuel.Oil,global.resource.Oil.name])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '] && global.tech.pillars === 1 ? Object.keys(global.pillars).length * 125000 + 1000000 : 0;\n                },\n            },\n            count(){\n                return Object.keys(races).length - 1;\n            },\n            on(){\n                return Object.keys(global.pillars).length;\n            },\n            effect(wiki){\n                let pillars = (wiki?.count ?? 0) + Object.keys(global.pillars).length;\n                if (pillars >= 1){\n                    return `<div>${loc(', ',[Object.keys(races).length - 1,pillars])}</div>`;\n                }\n                else {\n                    return `<div>${loc(', ',[Object.keys(races).length - 1])}</div>`;\n                }\n            },\n            action(args){\n                if (global.tech[', ',\n            hide_support: true,\n            prop(){\n                let desc = ` - <span class="has-text-advanced">${loc(', ')}:</span> <span class="has-text-caution">{{ on | filter(', ') }}</span>`;\n                desc = desc + ` - <span class="has-text-advanced">${loc(', ')}:</span> <span class="has-text-caution">{{ on | filter(', ') }}</span>`;\n                return desc;\n            },\n            filter(v,type){\n                let sup = hellSupression(', ') || global.portal.west_tower.count < size || wiki){\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[size])}</div>`;\n                }\n                else {\n                    return `<div>${loc(', ')}</div>`;\n                }\n            },\n            reqs: { hell_gate: 2 },\n            queue_size: 25,\n            queue_complete(){ return towerSize() - global.portal.west_tower.count; },\n            cost: {\n                Money(offset,wiki){ return ((offset || 0) + (global.portal.hasOwnProperty(', ') ? global.portal.west_tower.count : 0);\n                if (count < size){\n                    let remain = size - count;\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[remain])}</div><div class="has-text-caution">${loc(', ')}</div>`;\n                }\n                else {\n                    return loc(', ') || global.portal.east_tower.count < size || wiki){\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[size])}</div>`;\n                }\n                else {\n                    return `<div>${loc(', ')}</div>`;\n                }\n            },\n            reqs: { hell_gate: 2 },\n            queue_size: 25,\n            queue_complete(){ return towerSize() - global.portal.east_tower.count; },\n            cost: {\n                Money(offset,wiki){ return ((offset || 0) + (global.portal.hasOwnProperty(', ') ? global.portal.east_tower.count : 0);\n                if (count < size){\n                    let remain = size - count;\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[remain])}</div><div class="has-text-caution">${loc(', ')}</div>`;\n                }\n                else {\n                    return loc(', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_gate: 3 },\n            powered(){ return powerCostMod(6); },\n            cost: {\n                Money(offset){ return spaceCostMultiplier(', ');\n                if (unicornFathom > 0){\n                    security *= 1 + (traits.holy.vars(1)[1] / 100 * unicornFathom);\n                }\n                let min = global.tech.hell_gun >= 2 ? 65 : 40;\n                let max = global.tech.hell_gun >= 2 ? 100 : 60;\n                return `<div>${loc(', ',[Math.round(security)])}</div><div>${loc(', ',[min,max])}</div><div class="has-text-caution">${loc(', ',[$(this)[0].powered()])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_gate: 4 },\n            powered(){ return powerCostMod(5); },\n            powerBalancer(){\n                return [{ r: ', ', wiki);\n                return `<div>${loc(', ',[+(mining).toFixed(3)])}</div><div class="has-text-caution">${loc(', ',[$(this)[0].powered()])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '); },\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_lake: 3 },\n            powered(wiki){\n                let num_cooling_tower = wiki ? (global.portal?.cooling_tower?.on ?? 0) : p_on[', '] || 0) * (global.tech?.asphodel >= 13 ? 0.12 : 0.1);\n                }\n                for (const res of $(this)[0].res()){\n                    if (global.resource[res].display){\n                        let val = sizeApproximation(+(spatialReasoning($(this)[0].val(res) * multiplier)).toFixed(0),1);\n                        storage = storage + `<span>${loc(', ',[val,global.resource[res].name])}</span>`;\n                    }\n                };\n                storage = storage + ', ';\n                return `<div>${loc(', ',[1])}</div>${storage}<div class="has-text-caution">${loc(', ',[$(this)[0].powered(wiki)])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_lake: 6 },\n            powered(){ return powerCostMod(10); },\n            cost: {\n                Money(offset){ return spaceCostMultiplier(', '); },\n            },\n            effect(){\n                return `<div>${loc(', ',[8])}</div><div class="has-text-caution">${loc(', ',[$(this)[0].powered()])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')])}</div>`;\n            },\n            reqs: { hell_lake: 4 },\n            powered(){ return 0; },\n            s_type: ', '] && global.blood.spire >= 2 ? 20 : 15;\n                return `<div class="has-text-caution">${loc(', ')])}</div><div>${loc(', ',[rating])}</div><div class="has-text-caution">${loc(', ',[$(this)[0].ship.mil()])}</div>`;\n            },\n            ship: {\n                civ(){ return 0; },\n                mil(){ return global.race[', '); },\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')])}</div>`;\n            },\n            reqs: { hell_lake: 5 },\n            powered(){ return 0; },\n            s_type: ', '];\n                let bireme = +((rating ** num_on) * 100).toFixed(1);\n                return `<div class="has-text-caution">${loc(', ')])}</div><div>${loc(', '] && global.stats.achieve.what_is_best.e >= 4 ? 8 : 5])}</div><div class="has-text-danger">${loc(', ',[bireme])}</div><div class="has-text-caution">${loc(', ',[$(this)[0].ship.civ()])}</div>`;\n            },\n            special: true,\n            sAction(){\n                global.settings.civTabs = 4;\n                global.settings.marketTabs = 3;\n                if (!global.settings.tabLoad){\n                    loadTab(', ') || global.portal.oven.count < 100 || wiki){\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ', [100])}</div>` + (global.portal.hasOwnProperty(', ') && global.portal.oven.count >= 100 ? `<div class="has-text-special">${loc(', ')}</div>` : ``);\n                }\n            },\n            reqs: { dish:2 },\n            condition(){\n                return global.portal.oven.count < 100;\n            },\n            queue_size: 10,\n            queue_complete(){ return 100 - global.portal.oven.count; },\n            cost: {\n                Money(offset){ return ((offset || 0) + (global.portal.hasOwnProperty(', ') ? global.portal.oven.count : 0);\n                if (count < 100){\n                    let remain = 100 - count;\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[remain])}</div>`;\n                }\n                else {\n                    return fortressModules.prtl_lake.oven_complete.effect();\n                }\n            },\n            action(args){\n                if (global.portal.oven.count < 100 && payCosts($(this)[0])){\n                    global.portal[', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            wiki: false,\n            reqs: { dish: 3 },\n            condition(){\n                return global.portal.oven.count >= 100;\n            },\n            queue_complete(){ return 0; },\n            cost: {},\n            effect(wiki){\n                let fuel = $(this)[0].p_fuel();\n                return `<div>${loc(`portal_oven_desc`)}</div>${global.tech[', '] === 4 ? `<div class="has-text-special">${loc(', ')}</div>` : ``}<div class="has-text-caution">${loc(', ', [fuel.a, fuel.r])}</div>`;\n            },\n            powered(){ return powerCostMod(3500); },\n            p_fuel(){ return { r: ', '].done : 0);\n                return `<div>${loc(`portal_devilish_dish_desc`,[progress.toFixed(1)])}</div><div>${loc(`portal_devilish_dish_flavor${progress >= 100 ? 6 : Math.ceil(progress/20)}`)}</div>`;\n            },\n            action(args){\n                return false;\n            },\n            struct(){\n                return {\n                    d: { count: 0, done: 0, time: 0 },\n                    p: [', '); },\n            },\n            powered(){ return 0; },\n            effect(){\n                return `<div>${loc(', ')}</div><div class="has-text-danger">${loc(', '] ? -1 : 0)])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    global.portal[', '); },\n                Species(offset){ return popCost(10)}\n            },\n            powered(){ return 0; },\n            effect(){\n                return `<div>${loc(', ', [15])}</div><div class="has-text-danger">${loc(', ', [5])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    global.portal[', ',\n            prop(){\n                let desc = ` - <span class="has-text-advanced">${loc(', ')}:</span> <span class="has-text-caution">{{ supply | filter }} / {{ sup_max | filter }}</span>`;\n                return desc + ` (<span class="has-text-success">+{{ diff | filter(2) }}/s</span>)`;\n            },\n            filter(v,fix){\n                let val = fix ? +(v).toFixed(fix) : Math.floor(v);\n                return val.toLocaleString();\n            }\n        },\n        spire_mission: {\n            id: ', '); },\n            desc(){\n                return `<div>${global.race[', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_spire: 3 },\n            cost: {\n                Money(offset){ return spaceCostMultiplier(', '] / 50);\n                }\n                return +(base).toFixed(2);\n            },\n            effect(){\n                return `<div>${loc(', ',[$(this)[0].support()])}</div><div class="has-text-caution">${loc(', ',[$(this)[0].powered()])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_spire: 3 },\n            cost: {\n                Money(offset){ return spaceCostMultiplier(', '];\n                if (num_base_camps_on > 0){\n                    port_value *= 1 + (num_base_camps_on * 0.4);\n                }\n                return `<div class="has-text-caution">${loc(', ',[$(this)[0].support()])}</div><div>${loc(', ',[Math.round(port_value)])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', '),\n            desc(){\n                return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_spire: 4 },\n            cost: {\n                Money(offset){ return spaceCostMultiplier(', ',\n            support(){ return -1; },\n            effect(){\n                return `<div class="has-text-caution">${loc(', ',[$(this)[0].support()])}</div><div>${loc(', ',[40])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', ') || global.portal.bridge.count < 10 || wiki){\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[10])}</div>`;\n                }\n                else {\n                    return `<div>${loc(', ')}</div>`;\n                }\n            },\n            reqs: { hell_spire: 5 },\n            not_trait: [', ') ? global.portal.bridge.count : 0);\n                if (count < size){\n                    let remain = size - count;\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[remain])}</div><div class="has-text-caution">${loc(', ')}</div>`;\n                }\n                else {\n                    return loc(', '); },\n            desc(){\n                return `<div>${$(this)[0].title()}</div><div class="has-text-special">${loc(', ')}</div>`;\n            },\n            reqs: { hell_spire: 9 },\n            cost: {\n                Money(offset){ return spaceCostMultiplier(', ') ? global.portal.mechbay.max : 0;\n                return `<div class="has-text-caution">${loc(', ',[$(this)[0].support()])}</div><div>${loc(global.race[', ')}</div><div>${loc(', ',[bay,max])}</div>`;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', ') ? `<span class="has-text-warning">${loc(`portal_spire_type_${global.portal.spire.type}`)}</span>` : ', ') && Object.keys(global.portal.spire.status).length > 0){\n                    status = `<div>${loc(', ',[Object.keys(global.portal.spire.status).map(v => `<span class="has-text-warning">${loc(`portal_spire_status_${v}`)}</span>`).join(', ')])}</div>`;\n                }\n                let progress = global.portal.hasOwnProperty(', ') ? `<span class="has-text-warning">${+(global.portal.spire.progress).toFixed(3)}%</span>` : ', ';\n                let leftSide = `<div>${loc(', ',[floor])}</div><div>${loc(', ',[terrain])}</div>${status}<div>${loc(', ',[progress])}</div>`;\n                \n                let boss = global.portal.hasOwnProperty(', ';\n                let threat = `<div>${loc(', ',[`<span class="has-text-danger">${loc(`portal_mech_boss_${boss}`)}</span>`])}</div>`;\n\n                let weak = `???`;\n                let resist = `???`;\n                if (global.stats[', ']){\n                    let resists = bossResists(boss);\n                    let level = $(this)[0].mscan();\n                    if (level > 0){\n                        weak = loc(`portal_mech_weapon_${resists.w}`);\n                    }\n                    if (level >= 5){\n                        resist = loc(`portal_mech_weapon_${resists.r}`);\n                    }\n                }\n                let rightSide = `<div>${threat}<div>${loc(', ',[`<span class="has-text-warning">${weak}</span>`])}</div><div>${loc(', ',[`<span class="has-text-warning">${resist}</span>`])}</div></div>`;\n\n                return `<div class="split"><div>${leftSide}</div><div>${rightSide}</div></div>`;\n            },\n            mscan(){\n                let level = 0;\n                Object.keys(global.stats.spire).forEach(function(uni){\n                    let boss = global.portal.hasOwnProperty(', '] && global.tech.waygate < 2) || wiki){\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[10])}</div>`;\n                }\n                else {\n                    return `<div>${loc(', ')}</div>`;\n                }\n            },\n            reqs: { waygate: 1 },\n            condition(){\n                return global.tech[', ') ? `<span class="has-text-warning">${+(global.portal.waygate.progress).toFixed(3)}%</span>` : ', ';\n                    return `<div>${loc(', ')}</div><div>${loc(', ',[progress])}</div>`;\n                }\n                else {\n                    let size = 10;\n                    let remain = size - count;\n                    return `<div>${loc(', ')}</div><div class="has-text-special">${loc(', ',[remain])}</div>`;\n                }\n            },\n            action(args){\n                if (global.portal.waygate.count < 10 && global.tech[', '] && global.tech.edenic <= 2){\n                    return `<div>${loc(', ')}</div>`;\n                }\n                else {\n                    return `<div>${loc(', ')}</div>`;\n                }\n            },\n            action(args){\n                if (global.tech[', '); },\n            },\n            effect(wiki){\n                let vault = spatialReasoning(bank_vault() * (global.portal?.spire?.count || 1) / 3);\n                vault = +(vault).toFixed(0);\n                let containers = (global.portal?.spire?.count || 1) * 8;\n                let mon = (global.portal?.spire?.count || 1);\n\n                let desc = `<div>${loc(', ')])}</div>`;\n                desc += `<div>${loc(', ',[mon,loc(`arpa_project_monument_title`)])}</div>`;\n                desc += `<div>${loc(', ',[containers,global.resource.Crates.name])}</div><div>${loc(', ',[containers,global.resource.Containers.name])}</div>`;\n                desc += `<div>${loc(', ',[(global.portal?.spire?.count || 1)])}</div>`;\n\n                return desc;\n            },\n            action(args){\n                if (payCosts($(this)[0])){\n                    incrementStruct(', ');\n    clearElement(parent);\n    parent.append($(`<h2 class="is-sr-only">${loc(', ')}</h2>`));\n    if (!global.tech[', ']){\n                    parent.append(`<div id="${region}" class="space"><div id="sr${region}"><h3 class="name has-text-warning">${name}</h3>${property}</div></div>`);\n                }\n                else {\n                    parent.append(`<div id="${region}" class="space"><div id="sr${region}"><h3 class="name has-text-warning">${name}</h3> <span v-show="s_max">{{ support }}/{{ s_max }}</span>${property}</div></div>`);\n                }\n                vBind({\n                    el: `#sr${region}`,\n                    data: global.portal[support],\n                    filters: {\n                        filter(){\n                            return fortressModules[region].info.filter(...arguments);\n                        }\n                    }\n                });\n            }\n            else {\n                parent.append(`<div id="${region}" class="space"><div><h3 class="name has-text-warning">${name}</h3>${property}</div></div>`);\n            }\n\n            popover(region, function(){\n                    return typeof fortressModules[region].info.desc === ', ';\n    let fort = $(`<div id="${id}" class="fort"></div>`);\n    parent.append(fort);\n\n    let enemy = $(`<div v-for="(e, index) of enemy" :key="index" class="enemyFortress">\n        <div class="fortRow"><span class="has-text-success">{{ e.r | species }}</span><span class="has-text-warning">${loc(`fortress_wall`)} {{ e.f }}%</span></div>\n        <div class="fortRow second"><span class="has-text-caution">${loc(`fortress_demon_kills`)} {{ e.k | kills }}</span><a class="button" v-on:click="attack(index)" role="button">${loc(`civics_garrison_attack`)}</a></div>\n    </div>`);\n    fort.append(enemy);\n\n    vBind({\n        el: `#${id}`,\n        data: global.portal.throne,\n        methods: {\n            attack(idx){\n                let horde = Math.floor(global.portal.minions.spawns * seededRandom(6, 10, true) / 10);\n                let scale = global.race[', ']){\n                    died *= 1 - (traits.armored.vars()[0] / 100);\n                    died = Math.round(died);\n                }\n                let range = global.portal.throne.enemy[idx].f;\n                for (let i=0; i<range; i++){\n                    died += seededRandom(global.portal.throne.enemy[idx].s * 250 / rating, global.portal.throne.enemy[idx].s * 1250 / rating, true);\n                    if (horde > died){\n                        global.portal.throne.enemy[idx].f--;\n                    }\n                    else {\n                        break;\n                    }\n                }\n                died = Math.round(died);\n                if (global.portal.minions.spawns < died){ died = global.portal.minions.spawns; }\n                global.portal.minions.spawns -= died;\n                global.portal.throne.enemy[idx].k += died;\n                   \n                if (p_on[', ';\n    let fort = full ? $(`<div id="${id}" class="fort"></div>`) : $(', ');\n    if (full){\n        parent.append(fort);\n    }\n    else {\n        if (fort.length > 0){\n            clearElement(fort);\n        }\n        else {\n            fort = $(`<div id="${id}" class="fort gFort"></div>`);\n            parent.append(fort);\n        }\n        fort.append(`<div><h3 class="has-text-warning">${loc(', ')}</h3><button class="button observe right" @click="observation">${loc(', ')}</button></div>`);\n    }\n    \n\n    let status = $(', ');\n    fort.append(status);\n\n    let defense = $(`<span class="defense has-text-success" :aria-label="defense()">${loc(', ')} {{ f.garrison | defensive }}</span>`);\n    status.append(defense);\n    let activity = $(`<span class="has-text-danger pad hostiles" :aria-label="hostiles()">${loc(', ')} {{ f.threat }}</span>`);\n    status.append(activity);\n    let threatLevel = $(`<span class="pad threatLevel" :class="threaten()" :aria-label="threatLevel()">{{ f.threat | threat }}</span>`);\n    status.append(threatLevel);\n\n    let wallStatus = $(', ');\n    fort.append(wallStatus);\n\n    wallStatus.append($(`<span class="has-text-warning" :aria-label="defense()">${loc(', ')} <span :class="wall()">{{ f.walls }}%</span></span>`));\n\n    let station = $(`<div></div>`);\n    fort.append(station);\n    \n    station.append($(`<span>${loc(', ')}</span>`));\n    station.append($(', '));\n    \n    station.append($(`<span>${loc(', ')}</span>`));\n    station.append($(', '));\n\n    station.append($(`<span>${loc(', ')}</span>`));\n    station.append($(', '));\n\n    station.append($(`<span class="hireLabel"><button v-show="g.mercs" class="button merc" @click="hire" :aria-label="hireLabel()">${loc(', ')}</button></span>`));\n\n    var bunks = $(', ');\n    station.append(bunks);\n    bunks.append($(`<span class="has-text-warning">${loc(', ')}: </span>`));\n    let soldier_title = global.tech[', ');\n    bunks.append($(`<span><span class="soldier">${soldier_title}</span> <span v-html="$options.filters.stationed(g.workers)"></span> / <span>{{ g.max | s_max }} | <span></span>`));\n    bunks.append($(`<span v-show="g.crew > 0"><span class="crew">${loc(', ')}</span> <span>{{ g.crew }} | </span></span>`));\n    bunks.append($(`<span><span class="wounded">${loc(', ')}</span> <span>{{ g.wounded }}</span></span>`));\n\n    let color = global.settings.theme === ', ' ? ` type="is-light"` : ` type="is-dark"`;\n    let reports = $(`<div></div>`);\n    station.append(reports);\n    reports.append($(`<b-checkbox class="patrol" v-model="f.notify" true-value="Yes" false-value="No"${color}>${loc(', ')}</b-checkbox>`));\n    reports.append($(`<b-checkbox class="patrol" v-model="f.s_ntfy" true-value="Yes" false-value="No"${color}>${loc(', ')}</b-checkbox>`));\n    reports.append($(`<b-checkbox class="patrol" v-model="f.nocrew"${color} v-show="s.showGalactic">${loc(', ')}</b-checkbox>`));\n\n    if (full){\n        fort.append($(`<div class="training"><span>${loc(', ')} {{ g.rate, g.progress | trainTime }}</span><button class="button observe right" @click="observation">${loc(', ')}</button> <progress class="progress" :value="g.progress" max="100">{{ g.progress }}%</progress></div>`));\n    }\n\n    vBind({\n        el: `#${id}`,\n        data: {\n            f: global.portal.fortress,\n            g: global.civic.garrison,\n            s: global.settings\n        },\n        methods: {\n            defense(){\n                return loc(', ');\n            },\n            aNext(){\n                let inc = keyMultiplier();\n                if (global.portal.fortress.garrison < global.civic.garrison.workers){\n                    global.portal.fortress.garrison += inc;\n                    if (global.portal.fortress.garrison > global.civic.garrison.workers){\n                        global.portal.fortress.garrison = global.civic.garrison.workers;\n                    }\n                    global.portal.fortress[', ');\n                }\n            },\n            patInc(){\n                let inc = keyMultiplier();\n                if (global.portal.fortress.patrols * global.portal.fortress.patrol_size < global.portal.fortress.garrison){\n                    global.portal.fortress.patrols += inc;\n                    if (global.portal.fortress.garrison < global.portal.fortress.patrols * global.portal.fortress.patrol_size){\n                        global.portal.fortress.patrols = Math.floor(global.portal.fortress.garrison / global.portal.fortress.patrol_size);\n                    }\n                }\n            },\n            patDec(){\n                let dec = keyMultiplier();\n                if (global.portal.fortress.patrols > 0){\n                    global.portal.fortress.patrols -= dec;\n                    if (global.portal.fortress.patrols < 0){\n                        global.portal.fortress.patrols = 0;\n                    }\n                }\n            },\n            patSizeInc(){\n                let inc = keyMultiplier();\n                if (global.portal.fortress.patrol_size < global.portal.fortress.garrison){\n                    global.portal.fortress.patrol_size += inc;\n                    if (global.portal.fortress.garrison < global.portal.fortress.patrols * global.portal.fortress.patrol_size){\n                        global.portal.fortress.patrols = Math.floor(global.portal.fortress.garrison / global.portal.fortress.patrol_size);\n                    }\n                }\n            },\n            patSizeDec(){\n                let dec = keyMultiplier();\n                if (global.portal.fortress.patrol_size > 1){\n                    global.portal.fortress.patrol_size -= dec;\n                    if (global.portal.fortress.patrol_size < 1){\n                        global.portal.fortress.patrol_size = 1;\n                    }\n                }\n            },\n            wall(){\n                let val = global.portal.fortress.walls;\n                if (val >= 75){\n                    return "has-text-success";\n                }\n                else if (val <= 25){\n                    return "has-text-danger";\n                }\n                else {\n                    return "has-text-warning";\n                }\n            },\n            threaten(){\n                let val = global.portal.fortress.threat;\n                if (val < 1000){\n                    return "has-text-success";\n                }\n                else if (val >= 2000){\n                    return "has-text-danger";\n                }\n                else {\n                    return "has-text-warning";\n                }\n            },\n            hire(){\n                let repeats = keyMultiplier();\n                let canBuy = true;\n                while (canBuy && repeats > 0){\n                    let cost = mercCost();\n                    if (global.civic[', '] : 0;\n    let has_drop = false;\n    let wounded = 0;\n    if (global.civic.garrison.wounded > global.civic.garrison.workers - global.portal.fortress.garrison){\n        wounded = global.civic.garrison.wounded - (global.civic.garrison.workers - global.portal.fortress.garrison);\n        if (wounded > global.portal.fortress.garrison - (global.portal.fortress.patrols * global.portal.fortress.patrol_size)){\n            wounded -= global.portal.fortress.garrison - (global.portal.fortress.patrols * global.portal.fortress.patrol_size);\n            wounded /= global.portal.fortress.patrols;\n        }\n        else {\n            wounded = 0;\n        }\n    }\n    let brkpnt = +(wounded % 1).toFixed(10);\n    day_report.patrols = {};\n    console.log("\\n\\n")\n    for (let i=0; i<global.portal.fortress.patrols; i++){\n        let patrol_report = { encounter: false, droid: false, ambush: false, gem: 0, kills: 0, wounded: 0, died: 0};\n        let hurt = brkpnt > (1 / global.portal.fortress.patrols * i) ? Math.ceil(wounded) : Math.floor(wounded);\n        if (Math.rand(0,global.portal.fortress.threat) >= Math.rand(0,999)){\n            patrol_report.encounter = true;\n            let pat_size = global.portal.fortress.patrol_size;\n            if (terminators > 0){\n                patrol_report.droid = true;\n                pat_size += global.tech[', '] && global.race.ocularPowerConfig.p){\n                    global.race.ocularPowerConfig.ds += Math.round(killed * traits.ocular_power.vars()[1]);\n                }\n            }\n            else {\n                let killed = pat_rating;\n                if (demons <= killed){\n                    killed = demons;\n                }\n                else {\n                    dead += casualties(demons-killed,pat_armor,false,patrol_report);\n                }\n                patrol_report.kills = killed;\n                global.portal.fortress.threat -= killed;\n                global.stats.dkills += killed;\n                if (forgeOperating){\n                    global.portal.soul_forge.kills += killed;\n                    soulCapacitor(killed);\n                }\n                if (killed > 0){\n                    let div = 35 - Math.floor(p_on[', ']){\n        revive = Math.round(Math.rand(0,(dead / traits.revive.vars()[6]) + 0.25));\n        day_report.revived = revive;\n        day_report.stats.revived = revive;\n        global.civic.garrison.workers += revive;\n    }\n\n    // Soldier Rebalancing\n    if (global.civic.garrison.wounded > global.civic.garrison.workers){\n        global.civic.garrison.wounded = global.civic.garrison.workers;\n    }\n    let garrison_size = global.portal.fortress.nocrew ? global.civic.garrison.workers - global.civic.garrison.crew : global.civic.garrison.workers;\n    if (garrison_size < global.portal.fortress.garrison){\n        global.portal.fortress.garrison = garrison_size;\n    }\n    if (global.portal.fortress.garrison < global.portal.fortress.patrols * global.portal.fortress.patrol_size){\n        let patrol_start = global.portal.fortress.patrols;\n        global.portal.fortress.patrols = Math.floor(global.portal.fortress.garrison / global.portal.fortress.patrol_size);\n        day_report.patrols_lost = patrol_start - global.portal.fortress.patrols;\n    }\n\n    if (dead > 0 && global.portal.fortress.notify === ', ']);\n        }\n    }\n\n    // Siege Chance\n    if (global.portal.fortress.garrison > 0 && global.portal.fortress.siege > 0){\n        global.portal.fortress.siege--;\n    }\n    if (global.portal.fortress.siege <= 900 && global.portal.fortress.garrison > 0 && 1 > Math.rand(0,global.portal.fortress.siege)){\n        let siege_report = { destroyed: false, damage: 0, kills: 0, surveyors: 0, soldiers: 0};\n        let defense = fortressDefenseRating(global.portal.fortress.garrison);\n        let defend = defense / 35 > 1 ? defense / 35 : 1;\n        let siege = Math.round(global.portal.fortress.threat / 2);\n\n        let damage = 0;\n        let killed = 0;\n        let destroyed = false;\n        while (siege > 0 && global.portal.fortress.walls > 0){\n            let terminated = Math.round(Math.rand(1,defend + 1));\n            if (terminated > siege){\n                terminated = siege;\n            }\n            siege -= terminated;\n            global.portal.fortress.threat -= terminated;\n            global.stats.dkills += terminated;\n            if (forgeOperating){\n                global.portal.soul_forge.kills += terminated;\n                soulCapacitor(terminated);\n            }\n            killed += terminated;\n            if (siege > 0){\n                damage++;\n                global.portal.fortress.walls--;\n                if (global.portal.fortress.walls === 0){\n                    siege_report.destroyed = true;\n                    destroyed = true;\n                    break;\n                }\n            }\n        }\n        if (global.race[', ']);\n                    }\n                }\n                day_report.surveyors = dead;\n                day_report.stats.surveyors = dead;\n                global.civic.hell_surveyor.workers -= dead;\n                global.civic.hell_surveyor.max -= dead;\n                global.resource[global.race.species].amount -= dead;\n                global.portal.carport.damaged += dead;\n            }\n        }\n\n        day_report.surveyor_finds = {};\n        if (global.civic.hell_surveyor.workers > 0 && drone_kills > 0){\n            let drone_kills_left = drone_kills;\n            for (let i=0; i<global.civic.hell_surveyor.workers; i++){\n                let surv_report = { gem: 0, bodies: 0 };\n                // Avoid rounding error in total number of drone kills to distribute\n                let max_search_chance = Math.round(drone_kills_left / (global.civic.hell_surveyor.workers - i));\n                let min_search_chance = Math.round(max_search_chance / 2);\n                drone_kills_left -= max_search_chance;\n\n                // Each surveyor may search from 50% to 100% of 1 equal share of drone kills\n                let searched = Math.rand(min_search_chance, max_search_chance+1);\n                // Limit to 100 bodies per surveyor\n                let search_limit = highPopAdjust(100);\n                if (searched > search_limit){ searched = search_limit; }\n                surv_report.bodies = searched;\n                if (searched > 0){\n                    let div = 25 - Math.floor(p_on[', ']);\n                            }\n                            has_drop = true;\n                        }\n                    }\n                }\n                day_report.surveyor_finds[i+1] = surv_report;\n            }\n        } \n    }\n\n    if (!has_drop && global.portal.fortress.pity < 10000){\n        global.portal.fortress.pity++;\n    }\n\n    if (global.stats.dkills >= 1000000 && global.tech[', ') && global.eden.encampment.asc){\n                let heatSink = actions.interstellar.int_sirius.ascension_trigger.heatSink();\n                heatSink = heatSink < 0 ? Math.abs(heatSink) : 0;\n                if (heatSink > 0){\n                    souls *= 1 + (heatSink / 12500);\n                }\n            }\n            souls = Math.floor(souls * asphodelResist());\n            global.portal.soul_forge.kills += souls;\n            soulCapacitor(souls);\n        }\n\n        if (forgeOperating && global.tech[', '] && global.portal.minions.count > 0){\n        if ((global.portal.throne.enemy.length === 0 || \n            (global.portal.throne.spawned.length >= 3 && global.portal.throne.enemy.length <= 1) ||\n            (global.portal.throne.spawned.length >= 8 && global.portal.throne.enemy.length <= 2)\n        ) && Math.rand(0,10) === 0 && global.portal.minions.spawns > 0){\n            if (global.portal.throne.spawned.length === 0){\n                addHellEnemy([', '] ? 102 - traits.blurry.vars()[0] : 102;\n                reapEffect -= (global.portal?.reaper?.rank || 1) * 2;\n                if (reapEffect < 1){ reapEffect = 1; }\n                let reaper = 0.25 + (eRating * 0.01) - ((global.portal?.reaper?.count || 0) ** (1 + ((global.portal?.reaper?.rank || 1) - 1) / 25) / reapEffect);\n                if (reaper < 0.01){ reaper = 0.01; }\n                let bound = Math.round(global.portal.minions.spawns * (0.5 * eRating) * (eRating ** reaper) / rating);\n                let kills = Math.rand(e.s, bound);\n                if (kills > global.portal.minions.spawns){ kills = global.portal.minions.spawns; }\n                global.portal.minions.spawns -= kills;\n                e.k += kills;\n                if (forgeOperating){\n                    global.portal.soul_forge.kills += kills;\n                }\n\n                if (e.f < 100 && Math.rand(0, 10) === 0){\n                    e.f++;\n                }\n\n                if (global.race[', ');\n            }\n            else if (global.portal[s].rank < 5 && global.portal.throne.skill && global.portal.throne.points > 0){\n                $(`#portal-${s} a.button`).addClass(', ')} ${global.portal[struct]?.rank}</span>)`;\n}\n\nfunction addHellEnemy(type = [], allowRecursion = true, allowRepeat = false){\n    let invaders = [];\n    let current = global.portal.throne.enemy.map(e => e.r);\n    if (type.includes(', ',\n                equip: [],\n                infernal: false\n            };\n        }\n\n        let assemble = $(`<div id="mechAssembly" class="mechAssembly"></div>`);\n        lab.append(assemble);\n\n        let title = $(`<div><span class="has-text-caution">${loc(global.race[', '] ? `portal_mech_spawn` : `portal_mech_assembly`)}</span> - <span>{{ b.size | slabel }} {{ b.chassis | clabel }}</span></div>`);\n        assemble.append(title);\n\n        title.append(` | <span><span class="has-text-warning">${loc(global.race[', ')}</span>: {{ m.bay }} / {{ m.max }}</span>`);\n        title.append(` | <span><span class="has-text-warning">${loc(', ')}</span>: {{ p.supply | round }} / {{ p.sup_max }}</span>`);\n\n        let infernal = global.blood[', '] && global.blood.prepared >= 3 ? `<b-checkbox class="patrol" v-model="b.infernal">${loc(', ',[25])})</b-checkbox>` : ``;\n        assemble.append(`<div><span class="has-text-warning">${loc(global.race[', '] ? `portal_mech_lair` : `portal_mech_space`)}</span> <span class="has-text-danger">{{ b.size | bay }}</span> | <span class="has-text-warning">${loc(`portal_mech_cost`)}</span> <span class="has-text-danger">{{ b.size | price }}</span> | <span class="has-text-warning">${loc(`portal_mech_soul`,[global.resource.Soul_Gem.name])}</span> <span class="has-text-danger">{{ b.size | soul }}</span>${infernal}</div>`)\n        assemble.append(`<div>{{ b.size | desc }}</div>`);\n\n        let options = $(`<div class="bayOptions"></div>`);\n        assemble.append(options);\n\n        let sizes = ``;\n        let sizeTypes = global.race[', ')" class="size r0 a${idx}" data-val="${size}">${loc(`portal_mech_size_${size}`)}</b-dropdown-item>`;\n        });\n\n        options.append(`<b-dropdown :triggers="[', ']" aria-role="list">\n            <button class="button is-info" slot="trigger">\n                <span>${loc(`portal_mech_size`)}: {{ b.size | slabel }}</span>\n                <b-icon icon="menu-down"></b-icon>\n            </button>${sizes}\n        </b-dropdown>`);\n\n        let chassis = ``;\n        let typeList = [', ')" class="chassis r0 a${idx}" data-val="${val}">${loc(`portal_mech_chassis_${val}`)}</b-dropdown-item>`;\n        });\n\n        options.append(`<b-dropdown :triggers="[', ']" aria-role="list">\n            <button class="button is-info" slot="trigger">\n                <span>${loc(`portal_mech_type`)}: {{ b.chassis | clabel }}</span>\n                <b-icon icon="menu-down"></b-icon>\n            </button>${chassis}\n        </b-dropdown>`);\n\n        for (let i=0; i<4; i++){\n            let weapons = ``;\n            let weaponList = validWeapons(global.portal.mechbay.blueprint.size,global.portal.mechbay.blueprint.chassis,i);\n            weaponList.forEach(function(val,idx){\n                weapons += `<b-dropdown-item aria-role="listitem" v-on:click="setWep(', ',${i})" class="weapon r${i} a${idx}" data-val="${val}">${loc(`portal_mech_weapon_${val}`)}</b-dropdown-item>`;\n            });\n\n            options.append(`<b-dropdown :triggers="[', ']" aria-role="list" v-show="vis(${i})">\n                <button class="button is-info" slot="trigger">\n                    <span>${loc(`portal_mech_weapon`)}: {{ b.hardpoint[${i}] || ', ' | wlabel }}</span>\n                    <b-icon icon="menu-down"></b-icon>\n                </button>${weapons}\n            </b-dropdown>`);\n        }\n\n        let e_cap = global.blood[', ' | equipment }}</b-dropdown-item>`;\n            });\n\n            options.append(`<b-dropdown :triggers="[', ']" aria-role="list" v-show="eVis(${i})">\n                <button class="button is-info" slot="trigger">\n                    <span>${loc(global.race[', ' | equipment }}</span>\n                    <b-icon icon="menu-down"></b-icon>\n                </button>${equip}\n            </b-dropdown>`);\n        }\n\n        assemble.append(`<div class="mechAssemble"><button class="button is-info" slot="trigger" v-on:click="build()"><span>${global.race[', ')}</span></button></div>`);\n\n        vBind({\n            el: ', '\n                    });\n                }\n            }\n        });\n\n        let mechs = $(`<div id="mechList" class="sticky mechList"></div>`);\n        lab.append(mechs);\n        drawMechs();\n    }\n}\n\nexport function buildMechQueue(action){\n    let size = mechSize(action.bp.size);\n    let avail = global.portal.mechbay.max - global.portal.mechbay.bay;\n    if (avail >= size && payCosts(false, action.cost)){\n        buildMech(deepClone(action.bp,true));\n        return true;\n    }\n    return false;\n}\n\nfunction buildMech(bp, queue){\n    let mech = deepClone(bp);\n    global.portal.mechbay.mechs.push(mech);\n    global.portal.mechbay.bay += mechSize(mech.size);\n    global.portal.mechbay.active++;\n}\n\nexport function mechDesc(parent,obj){\n    let mech = obj.type;\n    let costs = mechCost(mech.size,mech.infernal,true);\n\n    var desc = $(`<div class="shipPopper"></div>`);\n    var mechPattern = $(`<div class="divider">${mech.infernal ? `${loc(', '}${loc(`portal_mech_size_${mech.size}`)} ${loc(`portal_mech_chassis_${mech.chassis}`)}</div>`);\n    parent.append(desc);\n    desc.append(mechPattern);\n\n    var cost = $(', ');\n    desc.append(cost);\n\n    let weapons = [];\n    mech.hardpoint.forEach(function(hp){\n        weapons.push(`<span class="has-text-danger">${loc(`portal_mech_weapon_${hp}`)}</span>`);\n    });\n    desc.append(`<div>${weapons.join(', ')}</div>`);\n\n    let equip = [];\n    mech.equip.forEach(function(eq){\n        let type = eq;\n        if (type === ', ';\n                    break;\n            }\n        }\n        equip.push(`<span class="has-text-warning">${loc(`portal_mech_equip_${type}`)}</span>`);\n    });\n    desc.append(`<div>${equip.join(', ')}</div>`);\n\n    let tc = timeCheck({ id: `${mech.size}${Math.rand(0,100)}` , cost: costs, doNotAdjustCost: true }, false, true);\n    Object.keys(costs).forEach(function (res){\n        if (costs[res]() > 0){\n            let label = res === ', ');\n            cost.append($(`<div class="${color}" data-${res}="${costs[res]()}">${label}${sizeApproximation(costs[res](),2)}</div>`));\n        }\n    });\n\n    if (tc && tc[', ']){\n        desc.append($(`<div class="divider"></div><div id="popTimer" class="flair has-text-advanced">{{ t | timer }}</div>`));\n        vBind({\n            el: ', ' ">\n        <a class="scrap" @click="scrap(index)" role="button">${loc(global.race[', ')}</a>\n        <span> | </span><span>${loc(global.race[', ')} #{{index + 1}}: </span>\n        <span class="has-text-caution">{{ mech.infernal ? "${loc(', ')} " : "" }}{{ mech | size }} {{ mech | chassis }}</span>\n        <div :class="', '+mech.size">\n          <div>\n            <template v-for="hp of mech.hardpoint">\n              <span> | </span>\n              <span class="has-text-danger">{{ hp | weapon }}</span>\n            </template>\n          </div>\n        </div>\n        <div :class="', '+mech.size">\n          <div>\n            <template v-for="eq of mech.equip">\n              <span> | </span>\n              <span class="has-text-warning">{{ eq, mech.size | equipment }}</span>\n            </template>\n          </div>\n        </div>\n      </div>`);\n\n    vBind({\n        el: ', '];\n        assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n        \n        if (global.portal.spire.count >= 25 && global.portal.spire.count <= 100){\n            let odds = 105 - global.portal.spire.count;\n            if (Math.floor(seededRandom(0,odds) <= 5)){\n                assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n            }\n        }\n        else if (global.portal.spire.count > 100 && global.portal.spire.count <= 250){\n            assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n            let odds = 260 - global.portal.spire.count;\n            if (Math.floor(seededRandom(0,odds) <= 10)){\n                assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n            }\n        }\n        else if (global.portal.spire.count > 250 && global.portal.spire.count <= 1000){\n            assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n            assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n            let odds = 1025 - global.portal.spire.count;\n            if (Math.floor(seededRandom(0,odds) <= 25)){\n                assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n            }\n        }\n        else if (global.portal.spire.count > 1000){\n            assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n            assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n            assignValidStatus(effects[Math.floor(seededRandom(0,effects.length))]);\n        }\n    }\n\n    let mobs = Object.keys(monsters).filter(function (k){\n        let exclude = Object.keys(monsters[k].nozone);\n        if (exclude.some(i => Object.keys(global.portal.spire.status).includes(i)) || exclude.includes(global.portal.spire.type)){\n            return false;\n        }\n        return true;\n    });\n    global.portal.spire.boss = mobs[Math.floor(seededRandom(0,mobs.length))];\n}\n\nfunction assignValidStatus(effect){\n    if (global.portal.spire.status[', ':\n                    rating *= 1.25;\n                    break;\n            }\n        }\n\n        let terrainFactor = terrainEffect(mech);\n\n        let effects = [];\n        Object.keys(global.portal.spire.status).forEach(function(effect){\n            effects.push(effect);\n            rating *= statusEffect(mech,effect);\n        });\n\n        rating *= terrainRating(mech,terrainFactor,effects);\n\n        rating /= global.portal.spire.count;\n        let damage = 0;\n        for (let i=0; i<mech.hardpoint.length; i++){\n            let effect = checkBossResist(global.portal.spire.boss,mech.hardpoint[i]);\n            damage += rating * weaponPower(mech,effect);\n        }\n        return damage;\n    }\n}\n\n\nexport function drawHellObservations(startup){\n    if (!global.settings.tabLoad && global.settings.civTabs !== ($(`#mainTabs > nav ul li`).length - 1) && !startup){\n        return;\n    }\n    let info = $(', ');\n    clearElement(info);\n    \n    let observe = $(`<div id="hellObservations"></div>`);\n    info.append(observe);\n    \n    observe.append(`<b-tabs id="hellTabs" class="resTabs" v-model="s.hellTabs" :animated="s.animated" @input="swapTab">\n        <b-tab-item id="h_Report">\n            <template slot="header">\n                <span>${loc(', ')}</span>\n            </template>\n        </b-tab-item>\n        <b-tab-item id="h_Analysis">\n            <template slot="header">\n                <span>${loc(', ')}</span>\n            </template>\n        </b-tab-item>\n    </b-tabs>`);\n    \n    vBind({\n        el: `#hellObservations`,\n        data: {\n            s: global.settings\n        },\n        methods: {\n            swapTab(tab){\n                if (!global.settings.tabLoad){\n                    clearElement($(`#h_Report`));\n                    clearElement($(`#h_Analysis`));\n                    switch (tab){\n                        case 0:\n                            drawHellReports();\n                            break;\n                        case 1:\n                            drawHellAnalysis();\n                            break;\n                    }\n                }\n                return tab;\n            }\n        }\n    });\n    \n    if (!global.settings.tabLoad){\n        switch (global.settings.hellTabs){\n            case 0:\n                drawHellReports();\n                break;\n            case 1:\n                drawHellAnalysis();\n                break;\n        }\n    }\n    else {\n        drawHellReports();\n        drawHellAnalysis();\n    }\n}\n\nfunction drawHellAnalysis(){\n    if (!global.settings.tabLoad && global.settings.hellTabs !== 1){\n        return;\n    }\n    let info = ($(`#h_Analysis`));\n    let stats = $(`<div id="hellAnalysis" class="vscroll"></div>`);\n    info.append(stats);\n    let bd_settings = $(`<div></div>`);\n    stats.append(bd_settings);\n    let analysis = $(`<div class="hellAnalysis"></div>`);\n    stats.append(analysis);\n    let breakdown = $(`<div class="hellAnalysis"></div>`);\n    analysis.append(breakdown);\n    \n    let totalAnal = $(`<div id="hellAnalysisTotal" class="analysisColumn"></div>`);\n    let partialAnal = $(`<div id="hellAnalysisPeriod" class="analysisColumn"></div>`);\n    breakdown.append(totalAnal);\n    breakdown.append(partialAnal);\n    \n    bd_settings.append(`\n        <div>\n            <h2 class="has-text-warning">${loc(', ')}</h2>\n        </div>\n        <div>\n            <b-checkbox v-model="s.expanded">${loc(', ')}</b-checkbox>\n            <b-checkbox v-model="s.average">${loc(', ')}</b-checkbox>\n            <b-checkbox v-show="r.hyper || r.slow" v-model="s.hyperSlow">${loc(', ')}</b-checkbox>\n        </div>\n        <div>\n            <b-radio v-model="s.display" native-value="game_days">${loc(', ')}</b-radio>\n            <b-radio v-model="s.display" native-value="seconds">${loc(', ')}</b-radio>\n            <b-radio v-model="s.display" native-value="minutes">${loc(', ')}</b-radio>\n            <b-radio v-model="s.display" native-value="hours">${loc(', ')}</b-radio>\n            <b-radio v-model="s.display" native-value="days">${loc(', ')}</b-radio>\n        </div>\n    `);\n    \n    vBind({\n        el: ', ',[num,loc(`hell_analysis_time_${units}_abbr`)])\n    };\n    \n    let drawStats = function(id,type){\n        if (!id){\n            return;\n        }\n        let elem = $(`#${id}`)\n        clearElement(elem);\n        \n        elem.append(`\n            <div><h2 class="has-text-warning">${loc(', ' + type)}</h2>${type === ', '}</div>\n            <div><h2 class="has-text-alert">{{ st.${type}.start | startLabel }}</h2></div>\n            <div><h2>{{ st.${type}.days, s.display | time }}</h2></div>\n            <div><h2>{{ st.${type}.kills, ', ', s.average | genericMulti }}</h2><h2 class="text-button has-text-advanced" aria-label="${loc(', ')">{{ s.dropKills | dropdownLabel }}</h2></div>\n            <div v-show="s.dropKills">\n                <div v-show="p.war_drone"><h2>{{ st.${type}.kills.drones, ', ', s.average | genericSub }}</h2></div>\n                <div><h2>{{ st.${type}.kills.patrols, ', ', s.average | genericSub }}</h2></div>\n                <div><h2>{{ st.${type}.kills.sieges, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="p.gun_emplacement"><h2>{{ st.${type}.kills.guns, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="p.soul_forge"><h2>{{ st.${type}.kills.soul_forge, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="p.gate_turret"><h2>{{ st.${type}.kills.turrets, ', ', s.average | genericSub }}</h2></div>\n            </div>\n            <div v-show="sg.display"><h2>{{ st.${type}.gems, ', ', s.average | genericMulti }}</h2><h2 class="text-button has-text-advanced" aria-label="${loc(', ')">{{ s.dropGems | dropdownLabel }}</h2></div>\n            <div v-show="sg.display && s.dropGems">\n                <div><h2>{{ st.${type}.gems.patrols, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="p.gun_emplacement"><h2>{{ st.${type}.gems.guns, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="p.soul_forge"><h2>{{ st.${type}.gems.soul_forge, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="p.soul_forge"><h2>{{ st.${type}.gems.crafted, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="p.gate_turret"><h2>{{ st.${type}.gems.turrets, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="p.war_drone && p.carport"><h2>{{ st.${type}.gems.surveyors, ', ', s.average | genericSub }}</h2></div>\n                <div v-show="e.soul_compactor"><h2>{{ st.${type}.gems.compactor, ', ', s.average | genericSub }}</h2></div>\n            </div>\n            <div><h2>{{ st.${type}.wounded, ', ', s.average | generic }}</h2></div>\n            <div><h2>{{ st.${type}.died, ', ', s.average | generic }}</h2></div>\n            <div v-show="r.revive"><h2>{{ st.${type}.revived, ', ', s.average | generic }}</h2></div>\n            <div><h2>{{ st.${type}.surveyors, ', ', s.average | generic }}</h2></div>\n            <div><h2>{{ st.${type}.sieges, ', ', s.average | generic }}</h2></div>\n        `);\n    \n        vBind({\n            el: `#${id}`,\n            data: {\n                st: global.portal.observe.stats,\n                s: global.portal.observe.settings,\n                p: global.portal,\n                r: global.race,\n                sg: global.resource.Soul_Gem,\n                e: global.eden,\n            },\n            methods: {\n                resetObservations(){\n                    Object.keys(global.portal.observe.stats.period).forEach(function(stat){\n                        if ([', ');\n    \n    stats = ($(`#hellAnalysis`));\n    let graphs = $(`<div></div>`);\n    stats.append(graphs);\n    graphs.append(`<div><h2 id="hellGraphCreator" class="text-button has-text-success" @click="createGraph()">${loc(', ')}</h2></div>`);\n    let graphArea = $(`<div id="hellGraphingArea" class="graphingArea"></div>`);\n    graphs.append(graphArea);\n    \n    vBind({\n        el: ', ').append($(`<p id="modalBoxTitle" class="has-text-warning modalTitle">${loc(', ')}</p>`));\n\n                        var body = $(', ').append(body);\n                        let creator = $(`<div class="graphCreator"></div>`);\n                        body.append(creator);\n\n                        let settings = {\n                            chartType: ', '\n                        }\n                        \n                        creator.append(`\n                            <div><h2 class="has-text-warning">${loc(', ')}</h2> <b-input v-model="s.name" :input="nameUpdate(s.name)"></b-input></div>\n                        `)\n                        creator.append(`\n                            <div>\n                                <div>\n                                    <h2 class="has-text-warning">${loc(', ')}</h2>\n                                </div>\n                                <div>\n                                    <b-radio v-model="s.chartType" native-value="pie" @click.native="dataOptions(', ')}</b-radio>\n                                </div>\n                            </div>\n                        `);\n                        \n                        let dataRegion = $(`<div id="graphDataSelection"></div>`);\n                        creator.append(dataRegion); \n                        dataRegion.append(`<div><h2 class="has-text-warning">${loc(', ')}</h2></div>`);\n                        Object.keys(global.portal.observe.stats).forEach(function(dataSet){\n                            [', '].forEach(function(group){\n                                dataRegion.append(`<div>\n                                    <b-radio v-show="${group === ', ',[loc(`hell_analysis_${dataSet}`),loc(`hell_analysis_${group}`)])}</b-radio>\n                                </div>`);\n                            });\n                        });\n\n                        creator.append(`\n                            <div>\n                                <div v-show="e.show">\n                                    <h2 class="has-text-danger">{{ e.message }}</h2>\n                                </div>\n                                <div>\n                                    <button class="button" @click="createGraph()">${loc(', ')}</button>\n                                </div>\n                            </div>\n                        `);\n                        \n                        vBind({\n                            el: `#specialModal`,\n                            data: {\n                                s: settings,\n                                e: error,\n                                sg: global.resource.Soul_Gem\n                            },\n                            methods: {\n                                nameUpdate(name){\n                                    if (settings.chartName !== name){\n                                        error.show = false;\n                                        settings.chartName = name;\n                                    }\n                                },\n                                dataOptions(type){\n                                    switch (type){\n                                        case ', '}));\n                                }\n                            }\n                        });\n                    }\n                }, 50);\n            }\n        }\n    });\n    \n    //Draw existing graphs.\n    Object.keys(global.portal.observe.graphs).forEach(function(id){\n        drawGraph(graphArea,global.portal.observe.graphs[id]);\n    });\n}\n\nfunction newGraph(name,type,labels,data,settings){\n    let id = `hellGraph-${global.portal.observe.graphID}`;\n    global.portal.observe.graphID++;\n    global.portal.observe.graphs[id] = {\n        id: id,\n        chartID: `${id}-chart`,\n        name: name,\n        type: type,\n        labels: labels,\n        data: data,\n        settings: settings\n    };\n    return id;\n}\n\nfunction drawGraph(info,graphInfo){\n    let id = graphInfo.id;\n    if (hell_graphs[id]){\n        hell_graphs[id].graph.destroy();\n    }\n    \n    let chartCont = $(`<div id="graph-${id}-container" class="graphContainer"></div>`);\n    info.append(chartCont);\n    chartCont.append(`<div id="graph-${id}-controls" class="graphControls">\n        <div>\n            <h2></h2>\n            <h2 class="text-button has-text-danger" @click="deleteGraph()">Delete</h2>\n        </div>\n        <div class="graphTitle">\n            <h2>${graphInfo.name}</h2>\n        </div>\n    </div>`);\n    let graph = $(`<div class="graph"></div>`);\n    chartCont.append(graph);\n    \n    vBind({\n        el: `#graph-${id}-controls`,\n        methods: {\n            deleteGraph(){\n                hell_graphs[id].graph.destroy();\n                delete hell_graphs[id];\n                delete global.portal.observe.graphs[id];\n                clearElement($(`#graph-${id}-container`),true);\n                return;\n            }\n        }\n    });\n    \n    let newChart = $(`<canvas id="${graphInfo.chartID}"></canvas>`);\n    graph.append(newChart);\n    \n    hell_graphs[id] = {\n        data: graphInfo.data\n    };\n    switch (graphInfo.type){\n        case ', '],\n                hoverOffset: 4\n            }]\n        },\n        options: {\n            plugins: {\n                legend: {\n                    display: false\n                }\n            }\n        }\n    });\n}\n\nfunction drawHellReports(){\n    if (!global.settings.tabLoad && global.settings.hellTabs !== 0){\n        return;\n    }\n    purgeReports();\n    \n    let list = ``;\n    \n    let info = ($(`#h_Report`));\n    let reports = $(`<div id="hellReport" class="hellReports"></div>`);\n    info.append(reports);\n    let reportListSection = $(`<div class="reportList vscroll"></div>`);\n    reports.append(reportListSection);\n    reportListSection.append(`<div id="hellReportLogTitle"><h2 class="has-text-info">${loc(', ')}</h2><span class="refresh" @click="updateList()" aria-label="${loc(', ')}">\n        <svg version="1.1" x="0px" y="0px" viewBox="0 0 492.883 492.883" enable-background="new 0 0 492.883 492.883" xml:space="preserve">\n            <path d="M122.941,374.241c-20.1-18.1-34.6-39.8-44.1-63.1c-25.2-61.8-13.4-135.3,35.8-186l45.4,45.4c2.5,2.5,7,0.7,7.6-3    l24.8-162.3c0.4-2.7-1.9-5-4.6-4.6l-162.4,24.8c-3.7,0.6-5.5,5.1-3,7.6l45.5,45.5c-75.1,76.8-87.9,192-38.6,282    c14.8,27.1,35.3,51.9,61.4,72.7c44.4,35.3,99,52.2,153.2,51.1l10.2-66.7C207.441,421.641,159.441,407.241,122.941,374.241z"/>\n\t\t    <path d="M424.941,414.341c75.1-76.8,87.9-192,38.6-282c-14.8-27.1-35.3-51.9-61.4-72.7c-44.4-35.3-99-52.2-153.2-51.1l-10.2,66.7    c46.6-4,94.7,10.4,131.2,43.4c20.1,18.1,34.6,39.8,44.1,63.1c25.2,61.8,13.4,135.3-35.8,186l-45.4-45.4c-2.5-2.5-7-0.7-7.6,3    l-24.8,162.3c-0.4,2.7,1.9,5,4.6,4.6l162.4-24.8c3.7-0.6,5.4-5.1,3-7.6L424.941,414.341z"/>\n        </svg>\n    </span></div>`);\n    let reportList = $(`<div id="hellReportList"></div>`);\n    reportListSection.append(reportList);\n    reports.append($(`<div id="hellReportDisplay" class="reportDisplay is-vertical vscroll"></div>`));\n    \n    let recentDay = { year: 0, day: 0 };\n    if (Object.keys(hell_reports).length){\n        recentDay.year = Object.keys(hell_reports)[0].split(', ')[1];\n        }\n        for (startYear; startYear<global.city.calendar.year; startYear++){\n            for (startDay; startDay<=orbitLength(); startDay++){\n                let gemString = ""; let gemCount = hell_reports[`year-${startYear}`][`day-${startDay}`].foundGems;\n                if (gemCount) {\n                    gemString = `<span class="has-text-advanced" aria-label="${loc(`hell_report_log_soul_gem_aria`)}">${gemCount >= 5 ? `&#9830x${gemCount}` : "&#9830".repeat(gemCount)}</span>`;\n                }\n                list = `\n                    <div class="text-button"><span @click="reportLoad(', ') + " " + startDay}${gemString}</span></div>\n                ` + list;\n            }\n            startDay = 1;\n        }\n        //Remaining days in current year.\n        for (startDay; startDay<global.city.calendar.day; startDay++){\n            let gemString = ""; let gemCount = hell_reports[`year-${startYear}`][`day-${startDay}`].foundGems;\n            if (gemCount) {\n                gemString = `<span class="has-text-advanced" aria-label="${loc(`hell_report_log_soul_gem_aria`)}">${gemCount >= 5 ? `&#9830x${gemCount}` : "&#9830".repeat(gemCount)}</span>`;\n            }\n            list = `\n                <div class="text-button"><span @click="reportLoad(', ') + " " + startDay}${gemString}</span></div>\n            ` + list;\n        }\n        recentDay.year = startYear;\n        recentDay.day = startDay;\n        \n        let reportList = ($(`#hellReportList`));\n        clearElement(reportList);\n        reportList.append(list);\n        vBind({\n            el: ', ',\n            methods: {\n                reportLoad(year,day){\n                    loadReport(year,day);\n                }\n            }\n        });\n    }\n\n    let loadReport = function(year,day){\n        if (!year || !day){\n            return;\n        }\n        let info = $(`#hellReportDisplay`);\n        clearElement(info);\n        let curr_report = hell_reports[`year-${year}`][`day-${day}`];\n\n        let statsBar = $(`<div id="hellReportStats" class="reportStats"></div>`);\n        info.append(statsBar);\n        let kills = 0;\n        let gems = 0;\n        Object.keys(curr_report.stats.kills).forEach(function(killType){\n            kills += curr_report.stats.kills[killType];\n        });\n        Object.keys(curr_report.stats.gems).forEach(function(gemType){\n            gems += curr_report.stats.gems[gemType];\n        });\n        statsBar.append(`<div><h2 class="has-text-info">${loc(', ',[year,day])}</h2></div>`);\n        statsBar.append(`<div>\n            <h2>${loc(', ',[kills])}</h2>\n            <h2 v-show="g.display">${loc(', ',[gems])}</h2>\n            <h2>${loc(', ',[curr_report.stats.wounded])}</h2>\n            <h2>${loc(', ',[curr_report.stats.died])}</h2>\n        </div>`);\n\n        info.append(`<div><h2 class="has-text-info">${loc(', ',[year,day])}</h2></div>`);\n        info.append(`<p class="has-text-danger">${loc(', ',[curr_report.start])}</p>`);\n\n        if (curr_report.soul_attractors){\n            info.append(`<p>${loc(', ',[curr_report.soul_attractors])}</p>`);\n        }\n        if (curr_report.ghost_trappers){\n            info.append(`<p>${loc(', ',[curr_report.ghost_trappers])}</p>`);\n        }\n        if (curr_report.soul_forge){\n            let displayText = $(`<p></p>`);\n            displayText.append(`<span>${loc(', ',[curr_report.soul_forge.kills])}</span>`);\n            if (curr_report.soul_forge.gem){\n                displayText.append(`<span class="has-text-success">${loc(', ',[global.resource.Soul_Gem.name,1])}</span>`);\n            }\n            if (curr_report.soul_forge.gem_craft){\n                displayText.append(`<span class="has-text-success">${loc(', ') : global.resource.Soul_Gem.name])}</span>`);\n            }\n            info.append(displayText);\n        }\n\n        if (curr_report.stats.gems.compactor){\n            let displayText = $(`<p></p>`);\n            if (curr_report.stats.gems.compactor){\n                displayText.append(`<span class="has-text-success">${loc(', ',[curr_report.stats.gems.compactor, global.resource.Soul_Gem.name])}</span>`);\n            }\n            info.append(displayText);\n        }\n\n        if (curr_report.drones){\n            Object.keys(curr_report.drones).forEach(function(num){\n                let drone = curr_report.drones[num];\n                let name = loc(', '),num]);\n                if (drone.encounter){\n                    info.append(`<p>${loc(', ',[name,drone.kills])}</p>`);\n                }\n                else {\n                    info.append(`<p class="has-text-warning">${loc(', ',[name])}</p>`);\n                }\n            });\n        }\n        if (curr_report.patrols){\n            Object.keys(curr_report.patrols).forEach(function(num){\n                let patrol = curr_report.patrols[num];\n                let name = loc(', ',[name]) : name;\n                if (patrol.encounter){\n                    let displayText = $(`<p></p>`);\n                    if (patrol.ambush){\n                        displayText.append(`<span class="has-text-warning">${loc(', ',[name,patrol.kills])}</span>`);\n                    }\n                    else {\n                        displayText.append(`<span>${loc(', ',[name,patrol.kills])}</span>`);\n                    }\n                    if (patrol.wounded){\n                        displayText.append(`<span class="has-text-danger">${patrol.wounded > 1 ? loc(', ')}</span>`);\n                    }\n                    if (patrol.died){\n                        displayText.append(`<span class="has-text-danger">${patrol.died > 1 ? loc(', ')}</span>`);\n                    }\n                    if (patrol.gem > 0){\n                        displayText.append(`<span class="has-text-success">${loc(', ',[global.resource.Soul_Gem.name,patrol.gem])}</span>`);\n                    }\n                    info.append(displayText);\n                }\n                else {\n                    info.append(`<p class="has-text-warning">${loc(', ',[name])}</p>`);\n                }\n            });\n        }\n\n        if (curr_report.surveyor_finds){\n            Object.keys(curr_report.surveyor_finds).forEach(function(num){\n                let surveyor = curr_report.surveyor_finds[num];\n                let name = loc(', '),num]);\n\n                let displayText = $(`<p></p>`);\n                displayText.append(`<span>${loc(', ',[name,surveyor.bodies])}</span>`);\n                if (surveyor.gem > 0){\n                    displayText.append(`<span class="has-text-success">${loc(', ',[global.resource.Soul_Gem.name,surveyor.gem])}</span>`);\n                }\n                info.append(displayText);\n            });\n        }\n\n        if (curr_report.revived){\n            info.append(`<p>${curr_report.revived > 1 ? loc(', ')}</p>`);\n        }\n        if (curr_report.patrols_lost){\n            info.append(`<p class="has-text-danger">${loc(', ',[curr_report.patrols_lost])}</p>`);\n        }\n        if (curr_report.siege){\n            if (curr_report.siege.destroyed){\n                info.append(`<p class="has-text-danger">${loc(', ',[curr_report.siege.surveyors,curr_report.siege.soldiers,curr_report.siege.kills])}</p>`);\n            }\n            else {\n                info.append(`<p class="has-text-warning">${loc(', ',[curr_report.siege.damage,curr_report.siege.kills])}</p>`);\n            }\n        }\n        if (curr_report.demons){\n            info.append(`<p class="has-text-danger">${loc(', ',[curr_report.demons])}</p>`);\n        }\n        if (curr_report.surveyors){\n            info.append(`<p class="has-text-danger">${curr_report.surveyors > 1 ? loc(', ')}</p>`);\n        }\n        if (curr_report.gun_emplacements){\n            Object.keys(curr_report.gun_emplacements).forEach(function(num){\n                let displayText = $(`<p></p>`);\n                let gun = curr_report.gun_emplacements[num];\n                let name = loc(', '),num]);\n                displayText.append($(`<span>${loc(', ')])}</span>`));\n                if (gun.gem){\n                    displayText.append(`<span class="has-text-success">${loc(', ',[global.resource.Soul_Gem.name,1])}</span>`);\n                }\n                info.append(displayText);\n            });\n        }\n        if (curr_report.gate_turrets){\n            Object.keys(curr_report.gate_turrets).forEach(function(num){\n                let displayText = $(`<p></p>`);\n                let turret = curr_report.gate_turrets[num];\n                let name = loc(', '),num]);\n                displayText.append(`<span>${loc(', ')])}</span>`);\n                if (turret.gem){\n                    displayText.append(`<span class="has-text-success">${loc(', ',[global.resource.Soul_Gem.name,1])}</span>`);\n                }\n                info.append(displayText);\n            });\n        }\n    \n        vBind({\n            el: ']